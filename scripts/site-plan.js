// Site plan page - placeholder external script
// Keeps the page compliant with the requirement for external JS files

document.addEventListener('DOMContentLoaded', function () {
    // No interactive behavior required for the site plan, but this file
    // ensures JavaScript is external and ready for future enhancements.
});
